{
  "type": "object",
  "properties": {
    "old_password": {
      "type": "string",
      "minLength": 6,
      "errorMessage": {
        "minLength": "Password should have at least 6 characters"
      }
    },
    "new_password": {
      "type": "string",
      "minLength": 6,
      "errorMessage": {
        "minLength": "New password should have at least 6 characters"
      }
    }
  },
  "required": ["old_password", "new_password"],
  "additionalProperties": false
}
